# TODO: Update Referral System in main.py

## Steps to Complete:
1. **Replace the existing referral logic in cmd_start function** with the new provided code.
   - Locate the section starting from "# ============================================================" (referral ID detection) to the end of the referral creation and menu display.
   - Replace with the new logic that includes improved ref_id parsing, separate user addition, additional checks for actual_ref, and better logging.

2. **Verify the changes**:
   - Ensure the code compiles without syntax errors.
   - Test the /start command with referral links to confirm proper referral creation and notifications.

3. **Test the bot**:
   - Run the bot and check if new users are handled correctly, referrals are created, and notifications are sent.
